---
title: 'hello, blog'
description: 'a placeholder post demonstrating the frontmatter shape for original writing.'
publishDate: 2026-07-17
---

This is an example post — delete `src/content/blog/example-post.md` once you're
ready to write something real.

This shape (no `original` field) is for writing that started here. Frontmatter
needed:

```
title: string
description: string (optional)
publishDate: date
draft: boolean (optional, defaults to false — set true to hide a post from
  the listing while you're still drafting it)
```

The body below the frontmatter is plain Markdown.
